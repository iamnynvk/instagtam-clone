import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const StoryCamera = () => {
  return (
    <View>
      <Text>StoryCamera</Text>
    </View>
  );
};

export default StoryCamera;

const styles = StyleSheet.create({});
